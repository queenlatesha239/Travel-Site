package com.techelevator;

public class Exercises {

	public static void main(String[] args) {

        /* Exercise 1
        1. 4 birds are sitting on a branch. 1 flies away. How many birds are left on
        the branch?
        */

		// ### Excercise 1 Answer
		int BirdsOnaBranch = 4;
		int BirdsFlyAway = 1;
		int BirdsRemaining = BirdsOnaBranch - BirdsFlyAway;
        System.out.println(BirdsRemaining);
        
        /* Exercise 2
        2. There are 6 birds and 3 nests. How many more birds are there than
        nests?
        */

		// ### Excercise 2 Answer
		int NumberOfBirds = 6;
		int NumberOfNest = 3;
		int NumberOfExtraBirds = NumberOfBirds - NumberOfNest;
        System.out.println(NumberOfExtraBirds);
        
        /* Exercise 3
        3. 3 raccoons are playing in the woods. 2 go home to eat dinner. How
        many raccoons are left in the woods?
        */

        // ### Exercise 3 Answer
        int RaccoonsInWoods = 3;
        int RaccoonsLeaveForDinner = 2;
        int RemainingRacoons= RaccoonsInWoods - RaccoonsLeaveForDinner;
        System.out.println(RemainingRacoons);

        /* Exercise 4
        4. There are 5 flowers and 3 bees. How many less bees than flowers?
        */

        // ### Exercise 4 Answer
        int flowers = 5;
        int bees = 3;
        int lessbees = flowers - bees;
        System.out.println(lessbees);

        /* Exercise 5
        5. 1 lonely pigeon was eating breadcrumbs. Another pigeon came to eat
        breadcrumbs, too. How many pigeons are eating breadcrumbs now?
        */

        // ### Excercise 5 Answer
        int LonelyPig = 1;
        int MateyPig = 1;
        int TotalPig = LonelyPig + MateyPig;
        System.out.println(TotalPig);

        /* Exercise 6
        6. 3 owls were sitting on the fence. 2 more owls joined them. How many
        owls are on the fence now?
        */

        // ### Exercise 6 Answer
        int OwlsSit = 3;
        int OwlsJoined = 2;
        int OwlsonFence = OwlsSit + OwlsJoined;
        System.out.println(OwlsonFence);

        /* Exercise 7
        7. 2 beavers were working on their home. 1 went for a swim. How many
        beavers are still working on their home?
        */
        
        // ### 7 Answer
        int TotalBeavs = 3;
        int SwimBeav = 1;
        int HomeBeav = TotalBeavs - SwimBeav;
        System.out.println(HomeBeav);

        /* Exercise 8
        8. 2 toucans are sitting on a tree limb. 1 more toucan joins them. How
        many toucans in all?
        */

        // ### Exercise 8 Answer
        int ToucansLimb = 2;
        int NewToucan = 1;
        int TotalToucans = ToucansLimb + NewToucan;
        System.out.println(TotalToucans);

        /* Exercise 9
        9. There are 4 squirrels in a tree with 2 nuts. How many more squirrels
        are there than nuts?
        */

        // ### Exercise 9 Answer
        int Squirrels = 4;
        int nuts = 2;
        int SqulessthanNuts = Squirrels - nuts;
        System.out.println(SqulessthanNuts);

        /* Exercise 10
        10. Mrs. Hilt found a quarter, 1 dime, and 2 nickels. How much money did
        she find?
        */

        // ### Exercise 10 Answer
        double quarter = 0.25;
        double dime = 0.10;
        double nickel = 0.05;
        double TotalforHilt = quarter + dime + (nickel * 2);
        System.out.println(TotalforHilt); 

        /* Exercise 11
        11. Mrs. Hilt's favorite first grade classes are baking muffins. Mrs. Brier's
        class bakes 18 muffins, Mrs. MacAdams's class bakes 20 muffins, and
        Mrs. Flannery's class bakes 17 muffins. How many muffins does first
        grade bake in all?
        */

        // ### Exercise 11 Answer
        int Brier = 18;
        int McAdams = 20;
        int Flannery = 17;
        int FirstGradeMuffins = Brier + McAdams + Flannery;
        System.out.println(FirstGradeMuffins);

        /* Exercise 12
        12. Mrs. Hilt bought a yoyo for 24 cents and a whistle for 14 cents. How
        much did she spend in all for the two toys?
        */

        double yoyo = 0.24;
        double whistle = 0.14;
        double MoneySpent = yoyo + whistle;
        System.out.println(MoneySpent);

        /* Exercise 13
        13. Mrs. Hilt made 5 Rice Krispies Treats. She used 8 large marshmallows
        and 10 mini marshmallows.How many marshmallows did she use
        altogether?
        */

        // ### Exercise 13 Answer
        int LargMarsh = 8;
        int MiniMarsh = 10;
        int TotalMarsh = LargMarsh + MiniMarsh;
        System.out.println(TotalMarsh);

        /* Exercise 14
        14. At Mrs. Hilt's house, there was 29 inches of snow, and Brecknock
        Elementary School received 17 inches of snow. How much more snow
        did Mrs. Hilt's house have?
        */

        // ### Exercise 14 Answer
        int HiltSnow = 29;
        int SchoolSnow = 17;
        int HiltHasMoreSnow = HiltSnow - SchoolSnow;
        System.out.println(HiltHasMoreSnow);

        /* Exercise 15
        15. Mrs. Hilt has $10. She spends $3 on a toy truck and $2 on a pencil
        case. How much money does she have left?
        */

        // Exercise 15 Answer
        int HiltMoney = 10;
        int truck = 3;
        int pencil = 2;
        int HiltChange = HiltMoney - truck - pencil;
        System.out.println(HiltChange);

        /* Exercise 16
        16. Josh had 16 marbles in his collection. He lost 7 marbles. How many
        marbles does he have now?
        */

        // ### Exercise 16 Answer
        int JoshMarbles = 16;
        int LostMarbles = 7;
        int RemainingMarbles = JoshMarbles - LostMarbles;
        System.out.println(RemainingMarbles);

        /* Exercise 17
        17. Megan has 19 seashells. How many more seashells does she need to
        find to have 25 seashells in her collection?
        */

        // Exercise 17 Answer
        int MegShells = 19;
        int TotalShells = 25;
        int CollectedShells = TotalShells - MegShells;
        System.out.println(CollectedShells);

        /* Exercise 18
        18. Brad has 17 balloons. 8 balloons are red and the rest are green. How
        many green balloons does Brad have?
        */

        // ### Excercise 18 Answer
        int TotalBalloons = 17;
        int RedBalloons = 8;
        int GreenBalloons = TotalBalloons - RedBalloons;
        System.out.println(GreenBalloons);

        /* Exercise 19
        19. There are 38 books on the shelf. Marta put 10 more books on the shelf.
        How many books are on the shelf now?
        */

        // ### Excercise 19 Answer
        int CurrentBooks = 38;
        int AddedBooks = 10;
        int TotalBooks = CurrentBooks + AddedBooks;
        System.out.println(TotalBooks);

        /* Exercise 20
        20. A bee has 6 legs. How many legs do 8 bees have?
        */

        // ### Exercise 20 Answer
        int OneBeeSix6Legs = 6;
        int EightBeesHowManyLegs = OneBeeSix6Legs * 8;
        System.out.println(EightBeesHowManyLegs);

        /* Exercise 21
        21. Mrs. Hilt bought an ice cream cone for 99 cents. How much would 2 ice
        cream cones cost?
        */

        // ### Exercise 21 Answer
        double ConeCost = 0.99;
        double TwoConeCost = ConeCost * 2;
        System.out.println(TwoConeCost);

        /* Exercise 22
        22. Mrs. Hilt wants to make a border around her garden. She needs 125
        rocks to complete the border. She has 64 rocks. How many more rocks
        does she need to complete the border?
        */

        // ### Exercise 22 Answer
        int TotalRocks = 125;
        int CurrentRocks = 64;
        int RocksNeededToComplete = TotalRocks - CurrentRocks;
        System.out.println(RocksNeededToComplete);

        /* Exercise 23
        23. Mrs. Hilt had 38 marbles. She lost 15 of them. How many marbles does
        she have left?
        */

        // ### Exercise 23 Answer
        int TotalMarbles = 38;
        int HiltLostMarbles = 15;
        int CurrentMarbles = TotalMarbles - HiltLostMarbles;
        System.out.println(CurrentMarbles);

        /* Exercise 24
        24. Mrs. Hilt and her sister drove to a concert 78 miles away. They drove 32
        miles and then stopped for gas. How many miles did they have left to drive?
        */

        // ### Exercise 24 Answer
        int ConcertMiles = 78;
        int GasMiles = 32;
        int RemainingMiles = ConcertMiles - GasMiles;
        System.out.println(RemainingMiles);

        /* Exercise 25
        25. Mrs. Hilt spent 1 hour and 30 minutes shoveling snow on Saturday
        morning and 45 minutes shoveling snow on Saturday afternoon. How
        much total time (in minutes) did she spend shoveling snow?
        */

        // ### Exercise 25 Answer
        int MorningSnow = 90;
        int EveningSnow = 45;
        int TotalMinutes = MorningSnow + EveningSnow;
        System.out.println(TotalMinutes);

        /* Exercise 26
        26. Mrs. Hilt bought 6 hot dogs. Each hot dog cost 50 cents. How much
        money did she pay for all of the hot dogs?
        */

        // ### Exercise 26 Answer
        double CostPerHotDog = 0.50;
        double CostofSixDogs = CostPerHotDog * 6;
        System.out.println(CostofSixDogs);

        /* Exercise 27
        27. Mrs. Hilt has 50 cents. A pencil costs 7 cents. How many pencils can
        she buy with the money she has?
        */

        // ### Exercise 28 Answer
        double CostPerPencil = 0.07;
        double HiltsCents = 0.50;
        Double PencilsBought = HiltsCents / CostPerPencil;
        System.out.println(PencilsBought);

        /* Exercise 28
        28. Mrs. Hilt saw 33 butterflies. Some of the butterflies were red and others
        were orange. If 20 of the butterflies were orange, how many of them
        were red?
        */

        // ### Exercise 28 Answer
        int TotalButterflies = 33;
        int OrangeButterflies = 20;
        int RedButterflies = TotalButterflies - OrangeButterflies;
        System.out.println(RedButterflies);

        /* Exercise 29
        29. Kate gave the clerk $1.00. Her candy cost 54 cents. How much change
        should Kate get back?
        */

        // ### Exercise 29 Answer
        double CandyCost = 0.54;
        double Dollar = 1.00;
        double KatesChange = Dollar - CandyCost;
        System.out.println(KatesChange);

        /* Exercise 30
        30. Mark has 13 trees in his backyard. If he plants 12 more, how many trees
        will he have?
        */

        // ### Exercise 30 Answer
        int CurrentTrees = 13;
        int AddTrees = 12;
        int TreesInBack = CurrentTrees + AddTrees;
        System.out.println(TreesInBack);

        /* Exercise 31
        31. Joy will see her grandma in two days. How many hours until she sees
        her?
        */

        // ### Exercise 31 Answer
        int HoursPerDay = 24;
        int HoursTwoDays = HoursPerDay * 2;
        System.out.println(HoursTwoDays);

        /* Exercise 32
        32. Kim has 4 cousins. She wants to give each one 5 pieces of gum. How
        much gum will she need?
        */

        // ### Exercise 32 Answer
        int Cousins = 4;
        int GumPieces = Cousins * 4;
        System.out.println(GumPieces);

        /* Exercise 33
        33. Dan has $3.00. He bought a candy bar for $1.00. How much money is
        left?
        */

        // ### Exercise 33 Answer
        double CandyBarCost = 1.00;
        double DanDollars = 3.00;
        double DanChange = DanDollars - CandyBarCost;
        System.out.println(DanChange);

        /* Exercise 34
        34. 5 boats are in the lake. Each boat has 3 people. How many people are
        on boats in the lake?
        */

        // ### Exercise 34 Answer
        int BoatsinLake = 5;
        int PeoplePerBoat = 3;
        int TotalPeople = BoatsinLake * PeoplePerBoat;
        System.out.println(TotalPeople);

        /* Exercise 35
        35. Ellen had 380 legos, but she lost 57 of them. How many legos does she
        have now?
        */

        // ### Exercise 35 Answer
        int TotalLegos = 380;
        int LostLegos = 57;
        int RemainingLegos = TotalLegos - LostLegos;
        System.out.println(RemainingLegos);

        /* Exercise 36
        36. Arthur baked 35 muffins. How many more muffins does Arthur have to
        bake to have 83 muffins?
        */

        // ### Exercise 36 Answers
        int TotalMuffins = 83;
        int BakedMuffins = 35;
        int NeededMuffins = TotalMuffins - BakedMuffins;
        System.out.println(NeededMuffins);

        /* Exercise 37
        37. Willy has 1400 crayons. Lucy has 290 crayons. How many more
        crayons does Willy have then Lucy?
        */

        // ### Exercise 37 Answers
        int WillyCrayons = 1400;
        int LucyCrayons = 290;
        int WillHasMore = WillyCrayons - LucyCrayons;
        System.out.println(WillHasMore);

        /* Exercise 38
        38. There are 10 stickers on a page. If you have 22 pages of stickers, how
        many stickers do you have?
        */

        // ### Exercise 38 Answer
        int StickersPerPage = 10;
        int TotalStickers = StickersPerPage * 22;
        System.out.println(TotalStickers);

        /* Exercise 39
        39. There are 100 cupcakes for 8 children to share. How much will each
        person get if they share the cupcakes equally?
        */

        // Exercise 39 Answer
        int Cupcakes = 100;
        int children = 8;
        int CupcakePerChild = Cupcakes / children;
        System.out.println(CupcakePerChild);

        /* Exercise 40
        40. She made 47 gingerbread cookies which she will distribute equally in
        tiny glass jars. If each jar is to contain six cookies, how many
        cookies will not be placed in a jar?
        */

        // ### Exercise 40 Answer
        int GingerbreadCookies = 47;
        int GingerCookiesPerJar = 6;
        int RemainingCookies = GingerbreadCookies % GingerCookiesPerJar;
        System.out.println(RemainingCookies); 

        /* Exercise 41
        41. She also prepared 59 croissants which she plans to give to her 8
        neighbors. If each neighbor received an equal number of croissants,
        how many will be left with Marian?
        */

        // ###3 Exercise 41 Answer
        int Croissants = 59;
        int Neighbors = 8;
        int RemainingCroissants = Croissants % Neighbors;
        System.out.println(RemainingCroissants);

        /* Exercise 42
        42. Marian also baked oatmeal cookies for her classmates. If she can
        place 12 cookies on a tray at a time, how many trays will she need to
        prepare 276 oatmeal cookies at a time?
        */

        // ### Exercise 42 Answer
        int OatmealCookies = 276;
        int CookiesPerTray = 12;
        int TraysNeeded = OatmealCookies / CookiesPerTray;
        System.out.println(TraysNeeded);

        /* Exercise 43
        43. Marian’s friends were coming over that afternoon so she made 480
        bite-sized pretzels. If one serving is equal to 12 pretzels, how many
        servings of bite-sized pretzels was Marian able to prepare?
        */

        // ### Exercise 43 Answer
        int TotalPretzels = 480;
        int PerServing = 12;
        int PreparedPretzels = TotalPretzels / PerServing;
        System.out.println(PreparedPretzels);

        /* Exercise 44
        44. Lastly, she baked 53 lemon cupcakes for the children living in the city
        orphanage. If two lemon cupcakes were left at home, how many
        boxes with 3 lemon cupcakes each were given away?
        */

        // ### Exercise 44 Answer
        int LemonCupcakes = 53;
        int CupcakesHome = 2;
        int CakesPerBox = 3;
        int CupcakeGivenAway = LemonCupcakes - CupcakesHome / CakesPerBox;
        System.out.println(CupcakeGivenAway);

        /* Exercise 45
        45. Susie's mom prepared 74 carrot sticks for breakfast. If the carrots
        were served equally to 12 people, how many carrot sticks were left
        uneaten?
        */

        // ### Exercise 45 Answer
        int CarrotSticks = 74;
        int PeopleSticks = 12;
        int Uneaten = CarrotSticks % PeopleSticks;
        System.out.println(Uneaten);

        /* Exercise 46
        46. Susie and her sister gathered all 98 of their teddy bears and placed
        them on the shelves in their bedroom. If every shelf can carry a
        maximum of 7 teddy bears, how many shelves will be filled?
        */

        // ### Exercise 46 Answer
        int Teddys = 98;
        int TeddyPerShelf = 7;
        int TeddyShelves = Teddys / TeddyPerShelf;
        System.out.println(TeddyShelves);

        /* Exercise 47
        47. Susie’s mother collected all family pictures and wanted to place all of
        them in an album. If an album can contain 20 pictures, how many
        albums will she need if there are 480 pictures?
        */

        // ### Exercise 47 Answer
        int FamilyPix = 480;
        int AlbumPage = 20;
        int AlbumsNeeded = FamilyPix / AlbumPage;
        System.out.println(AlbumsNeeded);

        /* Exercise 48
        48. Joe, Susie’s brother, collected all 94 trading cards scattered in his
        room and placed them in boxes. If a full box can hold a maximum of 8
        cards, how many boxes were filled and how many cards are there in
        the unfilled box?
        */

        // ### Exercise 48 Answer
        int TotalTradingCards = 94;
        int CardsPerBox = 8;
        int FilledBoxes = TotalTradingCards / CardsPerBox;
        int UnfilledBoxes = TotalTradingCards % CardsPerBox;
        System.out.println("There are " + FilledBoxes + " boxes filled and " + UnfilledBoxes + " cards in the last box.");

        /* Exercise 49
        49. Susie’s father repaired the bookshelves in the reading room. If he has
        210 books to be distributed equally on the 10 shelves he repaired,
        how many books will each shelf contain?
        */

        // ### 49 Answer
        int SusieFatherBooks = 210;
        int AmountofShelves = 10;
        int BooksPerShelf = SusieFatherBooks / AmountofShelves;
        System.out.println(BooksPerShelf);
    
        /* Exercise 50
        50. Cristina baked 17 croissants. If she planned to serve this equally to
        her seven guests, how many will each have?
        */
        
        // ### 50 Answer
        int CroissantsBaked = 17;
        int CristinaGuest = 7;
        int CrossiantPerGuest = CroissantsBaked / CristinaGuest;
        System.out.println(CrossiantPerGuest);

	    /* Exercise 51
	    51. Bill and Jill are house painters. Bill can paint a 12 x 14 room in 2.15 hours, while Jill averages
	    1.90 hours. How long will it take the two painters working together to paint 5 12 x 14 rooms?
	    Hint: Calculate the hourly rate for each painter, combine them, and then divide the total walls in feet by the combined hourly rate of the painters.
	    */

        // ### 51 Answer
        float BillAvg = 2.15f;
        float JillAvg = 1.90f;
        int TotalNumberRooms = 623;
        int WorkDayHours = 8;
        int BothAvg = (int) (((((BillAvg + JillAvg)/2)/2) * TotalNumberRooms / WorkDayHours) + 1);
        System.out.println("It will take Bill and Jill " + BothAvg + " days to paint 623 rooms given their average speed.");

	    /* Exercise 52
	    52. Create and assign variables to hold a first name, last name, and middle initial. Using concatenation,
		build an additional variable to hold the full name in the order of last name, first name, middle initial. The
		last and first names should be separated by a comma followed by a space, and the middle initial must end
		with a period. Use "Grace", "Hopper, and "B" for the first name, last name, and middle initial.
		Example: "John", "Smith, "D" —> "Smith, John D."
	    */

        // ### 52 Answer
        String FirstName = "Grace";
        String LastName = "Hopper";
        String MiddleName = "B";
        String FullName = LastName + FirstName + MiddleName;
        System.out.println(FullName);

	    /* Exercise 53
	    53. The distance between New York and Chicago is 800 miles, and the train has already travelled 537 miles.
	    What percentage of the trip as a whole number has been completed?
	    */

        // ### 53 Answer
        float NYtoChiMiles = 800f;
        float MilesTraveled = 537f;
        float PercentageOfTripTaken = (NYtoChiMiles / MilesTraveled) * 100;
        System.out.println(PercentageOfTripTaken);
	}

}
